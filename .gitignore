# Build artifacts. The generated-code directory lives inside them: generated code
# derives from the schema the way a .o derives from a .cpp — committing both
# guarantees that one day they'll disagree.
build/
build-*/
prefix/

# Generator output, for when someone runs it by hand outside the build.
generated/
generated_test/
